import react from "react";
import {View, Text, StyleSheet} from 'react-native';

const IndexScreen = () => {
    return(
        <Text>This is the IndexScreen</Text>
    );
};

const styles = StyleSheet.create({

});

export default IndexScreen;